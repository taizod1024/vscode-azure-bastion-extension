# Azure Bastion Tunnel Extension (Work In Progress)

## Usage

1. Open a terminal
   1. Install Azure CLI
2. Open VS Code
   1. Install the Azure Bastion Tunnel extension
   2. Configure the `azure-bastion-tunnel` settings
   3. Click the `Azure Bastion Tunnel` button in the status bar
   4. Connect via SSH
